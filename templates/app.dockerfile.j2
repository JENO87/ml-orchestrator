{# templates/app.dockerfile.j2 #}
# This is a child template for a specific application.

{% extends "base.dockerfile.j2" %}

{% block source_code %}
# This application only needs the cli.py file, so we copy it specifically.
COPY src/ml_orchestrator/cli.py /app/
{% endblock source_code %}

{% block command %}
# The command for this specific application.
CMD ["python", "/app/cli.py"]
{% endblock command %}
